import React, { useEffect, useState } from "react";
import { createRoot } from "react-dom/client";
import { changes, containers, createContainer, createObject, deleteObject, login, loginParams, logout, readObject, saveObject, serviceStatus, session, updateContainer, type Container, type Note, type Session } from "./api";
import { decryptContainerMeta, decryptNote, deriveAuthSecret, encryptContainerMeta, encryptNote, fromBase64, type NotePayload } from "./crypto";
import { deleteNote as deleteCachedNote, getNote, putNote } from "./storage";
import { applyStoredTheme, applyTheme, getStoredTheme, THEME_OPTIONS, type ThemeName } from "./theme";
import "./styles.css";

type AuthState = { username: string; authSecret: string; user: Session["user"] };

function App() {
  const [auth, setAuth] = useState<AuthState | null>(null);
  const [checking, setChecking] = useState(true);
  useEffect(() => { applyStoredTheme(); session().then((value) => setAuth({ username: "", authSecret: "", user: value.user })).catch(() => {}).finally(() => setChecking(false)); }, []);
  if (checking) return <main className="center">Loading KyNotes…</main>;
  return auth ? <Workspace auth={auth} onLogout={() => { void logout().finally(() => setAuth(null)); }} /> : <Login onLogin={setAuth} />;
}

function Login({ onLogin }: { onLogin: (auth: AuthState) => void }) {
  const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  async function submit(event: React.FormEvent) {
    event.preventDefault(); setError(""); setBusy(true);
    try {
      const params = await loginParams(username.trim());
      const authSecret = await deriveAuthSecret(password, params.loginSalt, params.iterations);
      const result = await login(username.trim(), authSecret);
      onLogin({ username: username.trim(), authSecret, user: result.user });
      setPassword("");
    } catch (error) { setError(error instanceof Error ? error.message : "Unable to sign in"); }
    finally { setBusy(false); }
  }
  return <main className="auth-page"><section className="auth-card"><div className="eyebrow">PRIVATE NOTES</div><h1>Keep the thread.</h1><p className="lede">Your notes are encrypted in this browser before they leave it.</p><form onSubmit={submit}><label>Username<input autoComplete="username" value={username} onChange={(event) => setUsername(event.target.value)} required /></label><label>Password<input type="password" autoComplete="current-password" value={password} onChange={(event) => setPassword(event.target.value)} required /></label>{error && <p className="error">{error}</p>}<button disabled={busy}>{busy ? "Unlocking…" : "Unlock KyNotes"}</button></form><p className="hint">The password is used in memory to derive your authentication and note-encryption keys. It is never stored.</p></section></main>;
}

function Workspace({ auth, onLogout }: { auth: AuthState; onLogout: () => void }) {
  const [items, setItems] = useState<Container[]>([]); const [names, setNames] = useState<Record<string, string>>({}); const [selected, setSelected] = useState<Container | null>(null); const [notes, setNotes] = useState<Note[]>([]); const [selectedNote, setSelectedNote] = useState<Note | null>(null); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const [view, setView] = useState<"workspace" | "settings" | "admin">("workspace");
  useEffect(() => { void loadContainers(); }, []);
  async function loadContainers() { try { const value = await containers(); const nextNames: Record<string, string> = {}; for (const item of value) { try { if (item.metaCiphertext) nextNames[item.id] = (await decryptContainerMeta(auth.authSecret, item.id, fromBase64(item.metaCiphertext))).name; } catch { /* metadata may belong to another key */ } } setNames(nextNames); setItems(value); if (value[0]) await selectContainer(value[0]); } catch (error) { setError(error instanceof Error ? error.message : "Unable to load workspaces"); } }
  async function selectContainer(container: Container) { setSelected(container); setSelectedNote(null); try { const result = await changes(container.id); const loaded: Note[] = []; for (const change of result.changes.filter((entry) => entry.kind === "object" && !entry.deleted)) { try { const object = await readObject(change.id); const payload = await decryptNote(auth.authSecret, container.id, object.bytes); loaded.push({ id: change.id, ...payload, version: object.version, updatedAt: new Date().toISOString() }); } catch { /* Ignore objects encrypted for another key until the device flow is available. */ } } setNotes(loaded); } catch (error) { setError(error instanceof Error ? error.message : "Unable to load notes"); } }
  async function newWorkspace() { const name = prompt("Workspace name", "My workspace")?.trim(); if (!name) return; setBusy(true); try { const container = await createContainer(); const encrypted = await encryptContainerMeta(auth.authSecret, container.id, name); const encoded = btoa(String.fromCharCode(...encrypted)); const result = await updateContainer(container.id, encoded, container.metaVersion); const named = { ...container, metaCiphertext: encoded, metaVersion: result.metaVersion, changeSeq: result.changeSeq }; setNames((value) => ({ ...value, [named.id]: name })); setItems((value) => [...value, named]); await selectContainer(named); } catch (error) { setError(error instanceof Error ? error.message : "Unable to create workspace"); } finally { setBusy(false); } }
  async function renameWorkspace() { if (!selected) return; const name = prompt("Workspace name", names[selected.id] || "Workspace")?.trim(); if (!name) return; setBusy(true); try { const encrypted = await encryptContainerMeta(auth.authSecret, selected.id, name); const result = await updateContainer(selected.id, btoa(String.fromCharCode(...encrypted)), selected.metaVersion); const next = { ...selected, metaCiphertext: btoa(String.fromCharCode(...encrypted)), metaVersion: result.metaVersion, changeSeq: result.changeSeq }; setNames((value) => ({ ...value, [selected.id]: name })); setItems((value) => value.map((entry) => entry.id === next.id ? next : entry)); setSelected(next); } catch (error) { setError(error instanceof Error ? error.message : "Unable to rename workspace"); } finally { setBusy(false); } }
  async function newNote() { if (!selected) return; setBusy(true); try { const object = await createObject(selected.id); const note = { id: object.id, title: "Untitled note", body: "", version: 0, updatedAt: new Date().toISOString() }; setNotes((value) => [note, ...value]); setSelectedNote(note); } catch (error) { setError(error instanceof Error ? error.message : "Unable to create note"); } finally { setBusy(false); } }
  async function save(note: Note) { if (!selected) return; setBusy(true); try { const payload: NotePayload = { title: note.title, body: note.body }; const encrypted = await encryptNote(auth.authSecret, selected.id, payload); const result = await saveObject(note.id, encrypted, note.version); const saved = { ...note, version: result.version, updatedAt: new Date().toISOString() }; await putNote({ id: saved.id, containerID: selected.id, version: saved.version, payload: encrypted, updatedAt: saved.updatedAt }); setNotes((value) => value.map((entry) => entry.id === saved.id ? saved : entry)); setSelectedNote(saved); } catch (error) { setError(error instanceof Error ? error.message : "Unable to save note"); } finally { setBusy(false); } }
  async function remove(note: Note) { if (!confirm("Delete this note?")) return; try { await deleteObject(note.id); await deleteCachedNote(note.id); setNotes((value) => value.filter((entry) => entry.id !== note.id)); setSelectedNote(null); } catch (error) { setError(error instanceof Error ? error.message : "Unable to delete note"); } }
  const workspaceName = (container: Container) => names[container.id] || `Workspace ${container.id.slice(4, 10)}`;
  return <main className="app-shell"><header className="topbar"><div className="brand"><span className="brand-mark">K</span><span>KyNotes</span></div><div className="top-actions"><span className="sync-dot">● Local key active</span><button className="quiet" onClick={() => setView("settings")}>Settings</button>{auth.user.role === "admin" && <button className="quiet" onClick={() => setView("admin")}>Admin</button>}<button className="quiet" onClick={onLogout}>Lock</button></div></header>{view === "workspace" ? <div className="workspace"><aside className="sidebar"><div className="section-label">WORKSPACES</div>{items.map((container) => <button className={`nav-item ${selected?.id === container.id ? "selected" : ""}`} key={container.id} onClick={() => void selectContainer(container)}><span className="nav-icon">◈</span><span>{workspaceName(container)}</span></button>)}<button className="new-workspace" disabled={busy} onClick={() => void newWorkspace()}>＋ New workspace</button>{selected && <button className="new-workspace" disabled={busy} onClick={() => void renameWorkspace()}>✎ Rename workspace</button>}<div className="sidebar-bottom"><div className="section-label">ACCOUNT</div><div className="account-chip"><span className="avatar">{auth.user.id.slice(-2)}</span><span>{auth.username || auth.user.id.slice(0, 12)}</span></div></div></aside><section className="note-list"><div className="list-header"><div><div className="section-label">WORKSPACE</div><h2>{selected ? workspaceName(selected) : "Select a workspace"}</h2></div><button className="icon-button" disabled={!selected || busy} onClick={() => void newNote()}>＋</button></div>{notes.map((note) => <button className={`note-row ${selectedNote?.id === note.id ? "selected" : ""}`} key={note.id} onClick={() => setSelectedNote(note)}><strong>{note.title || "Untitled note"}</strong><span>{note.body.slice(0, 64) || "Empty note"}</span></button>)}{selected && notes.length === 0 && <div className="empty-list">No notes yet.<br />Create the first one.</div>}</section><section className="editor"><div className="editor-meta"><span>{selectedNote ? `Version ${selectedNote.version || "draft"}` : "Ready"}</span><span className="encrypted">Encrypted locally</span></div>{selectedNote ? <><input className="title-input" value={selectedNote.title} onChange={(event) => setSelectedNote({ ...selectedNote, title: event.target.value })} /><textarea className="body-input" value={selectedNote.body} onChange={(event) => setSelectedNote({ ...selectedNote, body: event.target.value })} placeholder="Start writing…" /><div className="editor-actions"><button className="danger quiet" onClick={() => void remove(selectedNote)}>Delete</button><button disabled={busy} onClick={() => void save(selectedNote)}>{busy ? "Saving…" : "Save encrypted note"}</button></div></> : <div className="empty-editor"><div className="empty-glyph">✦</div><h2>Your private desk.</h2><p>Select a note or create one. The server receives ciphertext only.</p>{selected && <button onClick={() => void newNote()}>Create a note</button>}</div>}</section></div> : <SettingsView admin={view === "admin"} onBack={() => setView("workspace")} />}{error && <button className="toast error" onClick={() => setError("")}>{error} ×</button>}</main>;
}

function SettingsView({ admin, onBack }: { admin: boolean; onBack: () => void }) {
  const [theme, setTheme] = useState<ThemeName>(getStoredTheme()); const [status, setStatus] = useState<{ health: boolean; ready: boolean } | null>(null); const [loading, setLoading] = useState(false);
  async function checkStatus() { setLoading(true); try { setStatus(await serviceStatus()); } finally { setLoading(false); } }
  return <section className="settings-layout"><aside className="settings-sidebar"><button className="quiet" onClick={onBack}>← Workspace</button><div className="section-label">{admin ? "ADMIN SETTINGS" : "SETTINGS"}</div><div className="settings-nav"><span className="selected">Appearance</span><span>Workspace</span>{admin && <span>Server</span>}</div></aside><div className="settings-content"><div className="settings-header"><div className="section-label">{admin ? "ADMIN" : "PREFERENCES"}</div><h1>{admin ? "Admin settings" : "Settings"}</h1><p>KyPost-inspired controls for the browser client and server.</p></div><section className="config-card"><h2>Appearance</h2><p className="config-muted">Choose from the complete KyNotes color selection. This preference stays in this browser.</p><label className="field"><span>Theme</span><select value={theme} onChange={(event) => setTheme(event.target.value as ThemeName)}>{THEME_OPTIONS.map((option) => <option key={option}>{option}</option>)}</select></label><button onClick={() => { applyTheme(theme); }}>Apply theme</button></section>{admin && <section className="config-card"><h2>Server</h2><p className="config-muted">Admin-only service checks. Configuration remains host-managed until a server settings API is added.</p><button onClick={() => void checkStatus()} disabled={loading}>{loading ? "Checking…" : "Check service status"}</button>{status && <p className="status-line">Health: {status.health ? "OK" : "failed"} · Ready: {status.ready ? "OK" : "failed"}</p>}</section>}<section className="config-card"><h2>Workspace privacy</h2><p className="config-muted">Workspace names are encrypted metadata. The server stores and returns ciphertext, never the name itself.</p></section></div></section>;
}

createRoot(document.getElementById("root")!).render(<React.StrictMode><App /></React.StrictMode>);
